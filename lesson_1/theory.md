### Packages

Любая програма на Go состоит из пакетов. Выполнение программы начинается с пакета main, то-есть компилятор ищет именно его как точку входа. 

Общепринятое правило - имя пакета должно совпадать с именем папки, а которой лежат файлы этого пакета.